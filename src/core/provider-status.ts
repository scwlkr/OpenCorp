export type ProviderId='local'|'openrouter'|'groq'|'gemini'|'zai';
export interface ProviderTestResult {provider:ProviderId;modelId:string;status:'passed'|'failed';startedAt:string;finishedAt:string;responsePassed:boolean;toolPassed:boolean;requests:number;latencyMs:number;httpStatuses:number[];errorCode?:string;quotaHeaders?:Record<string,string>}
export interface ProviderStatus {id:ProviderId;configured:boolean;health:'green'|'red'|'unknown';reason:string;modelIds:string[];activeRuns:number;audit:{status:'unknown'|'owner-attested'|'free-pricing-required';verifiedAt?:string;expiresAt?:string};quota:{status:'unknown';observedAt?:string;modelId?:string;headers?:Record<string,string|number>};lastTest?:ProviderTestResult;cooldown?:{retryAt:string;basis:string};testing:boolean}
